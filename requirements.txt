# STAC COPC Catalog - Python Dependencies

# Core STAC libraries
pystac>=1.9.0
pystac-client>=0.7.0
stactools>=0.5.0

# Geospatial utilities
pyproj>=3.6.0
shapely>=2.0.0

# AWS SDK
boto3>=1.34.0

# Validation
jsonschema>=4.20.0

# Utilities
python-dotenv>=1.0.0
click>=8.1.0
tqdm>=4.66.0

# Note: PDAL Python bindings (pdal>=3.2.0) should be installed via conda:
#   conda install -c conda-forge pdal python-pdal
# Or use PDAL CLI tools directly (installed via brew or system package manager)
